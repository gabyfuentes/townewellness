import { Component, OnInit, ElementRef, transition } from '@angular/core';
import { animate, trigger, state, query, style } from '@angular/animations';

@Component({
  selector: 'app-carousel',
  templateUrl: './carousel.component.html',
  styleUrls: ['./carousel.component.css'],
})
export class CarouselComponent implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}
